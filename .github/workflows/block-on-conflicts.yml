name: Block Merge on "no-deploy" Label

on:
  pull_request:
    types: [labeled, unlabeled, opened, synchronize, reopened]

jobs:
  check-label:
    runs-on: ubuntu-latest
    steps:
      - name: Check for "no-deploy" label
        uses: actions/github-script@v7
        with:
          script: |
            const labels = context.payload.pull_request.labels.map(l => l.name);
            if (labels.includes('no-deploy')) {
              core.setFailed('Merge blocked: PR contains "no-deploy" label.');
            } else {
              core.info('PR does not contain "no-deploy" label. OK to merge.');
            }